# Base URL for the backend API. In local dev, Vite's proxy (see vite.config.js)
# forwards /api requests to http://localhost:5000, so this can stay as /api.
# In production (separate frontend/backend hosts), set this to the full backend URL.
VITE_API_URL=/api
